services:
  meu-portal-verde:
    build:
      context: .
      dockerfile: Dockerfile
    image: personal-landing:latest
    container_name: personal-landing-page
    restart: unless-stopped
    ports:
      - "8080:80"
